{
  "name": "src",
  "version": "1.0.0",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "build": "tsc",
    "start": "npm run build && func start",
    "clean": "rm -rf dist",
    "test:unit": "jest tests/unit",
    "test:integration": "npm run build && jest /tests/integration/integration.test.ts --runInBand --detectOpenHandles",
    "test": "npm run test:unit && npm run test:integration"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@azure/functions": "^3.5.1",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.10.0",
    "@types/supertest": "^6.0.3",
    "copyfiles": "^2.4.1",
    "jest": "^30.2.0",
    "supertest": "^7.1.4",
    "ts-jest": "^29.4.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
